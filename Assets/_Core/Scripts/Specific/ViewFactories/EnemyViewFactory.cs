public class EnemyViewFactory : BaseMonoViewFactory<EnemyModel, EnemyView>
{
	protected override void OnViewConstructedForModel(EnemyModel model, EnemyView view)
	{
		// TODO: Setup Enemy View (sprite etc) depending on the model settings.
	}
}
