public class EnemyViewFactory : BaseMonoViewFactory<CharacterModel, CharacterView>
{
	protected override void OnViewConstructedForModel(CharacterModel model, CharacterView view)
	{
		// TODO: Setup Enemy View (sprite etc) depending on the model settings.
	}
}
